#include "User.hpp"
#include "Data.hpp"

class Game {
private:

    // User has unit32_t id, and string name
    std::vector<User> users;    
    //Data data;
public:

    // function that sends out a message to all the users

    // function that accepts input from all the users



};